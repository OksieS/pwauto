/* let shopOffers = [];
shopOffers.push(
  ["a", "b", "c", "d", "e"],
  [1, 2, 3, 4, 5],
  ["one", "two", "three"],
);
let bundles = [];
let items = [];

for (let i = 0; i < shopOffers.length; i++) {
  //console.log(a[i]);
  bundles = shopOffers[i];
  for (let k = 0; k < bundles.length; k++) {
    items.push(bundles[k]);
  }
}
items.forEach((item) => {
  console.log(`Do something with ${item}`);
}); */
//console.log(items);

// for loop
let x = 10;
let i;
for (i = 0; i <= x; i++) {
  console.log(i);
}
